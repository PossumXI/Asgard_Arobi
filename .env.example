# ASGARD Environment Variables
# ===========================================================================
# SECURITY WARNING: 
#   - Copy this file to .env and fill in your ACTUAL values
#   - NEVER commit .env to version control
#   - Generate strong, unique passwords for production
#   - Rotate all secrets regularly
# ===========================================================================

# Environment Mode (development | production)
# In production, all secrets are REQUIRED - app will not start without them
ASGARD_ENV=development

# ---------------------------------------------------------------------------
# Stripe Configuration (REQUIRED for payments)
# Get keys from: https://dashboard.stripe.com/apikeys
# ---------------------------------------------------------------------------
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_SUCCESS_URL=https://aura-genesis.org/dashboard?success=true
STRIPE_CANCEL_URL=https://aura-genesis.org/pricing
STRIPE_PORTAL_RETURN_URL=https://aura-genesis.org/dashboard

# ---------------------------------------------------------------------------
# Database Configuration (REQUIRED)
# Generate strong passwords: openssl rand -base64 32
# ---------------------------------------------------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=55432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=asgard
POSTGRES_SSLMODE=disable

MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_USER=admin
MONGO_PASSWORD=
MONGO_DB=asgard

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# NATS Configuration
NATS_URL=nats://localhost:4222

# ---------------------------------------------------------------------------
# JWT Secret (REQUIRED - minimum 32 characters)
# Generate: openssl rand -base64 48
# ---------------------------------------------------------------------------
ASGARD_JWT_SECRET=

# ---------------------------------------------------------------------------
# WebSocket Security
# Comma-separated list of allowed origins for WebSocket connections
# ---------------------------------------------------------------------------
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# ---------------------------------------------------------------------------
# External APIs (optional)
# ---------------------------------------------------------------------------
# N2YO API (for orbital tracking)
N2YO_API_KEY=

# WebRTC Configuration
STUN_SERVER=stun:stun.l.google.com:19302
TURN_SERVER=
TURN_USERNAME=
TURN_PASSWORD=

# ---------------------------------------------------------------------------
# Frontend Configuration
# ---------------------------------------------------------------------------
FRONTEND_URL=http://localhost:5173
ASGARD_ADMIN_PORTAL_URL=https://aura-genesis.org/dashboard/admin

# ---------------------------------------------------------------------------
# Email Configuration (optional - for email verification)
# ---------------------------------------------------------------------------
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=Gaetano@aura-genesis.org
SMTP_FROM_NAME=ASGARD

# ---------------------------------------------------------------------------
# Access Code Rotation
# ---------------------------------------------------------------------------
ACCESS_CODE_ROTATION_HOURS=24
ACCESS_CODE_ROTATION_CHECK_MINUTES=15

# ---------------------------------------------------------------------------
# Admin Bootstrap (optional - one-time initialization)
# ---------------------------------------------------------------------------
ASGARD_BOOTSTRAP_ADMIN_EMAIL=
ASGARD_BOOTSTRAP_ADMIN_PASSWORD=
ASGARD_BOOTSTRAP_ADMIN_FULL_NAME=
# Force a new code on each server start (rotation still runs every 24h)
ASGARD_BOOTSTRAP_ROTATE_ON_START=false
ASGARD_BOOTSTRAP_OUTPUT_PATH=Documentation/Bootstrap_Access.md

# ---------------------------------------------------------------------------
# WebAuthn/FIDO2 Configuration (for government portal)
# ---------------------------------------------------------------------------
ASGARD_WEBAUTHN_RP_ID=localhost
ASGARD_WEBAUTHN_RP_NAME=ASGARD Government Portal
ASGARD_WEBAUTHN_RP_ORIGIN=http://localhost:5173

# ---------------------------------------------------------------------------
# Security Scanner Configuration (Giru)
# ---------------------------------------------------------------------------
SECURITY_SCANNER_MODE=pcap
SECURITY_LOG_SOURCES=/var/log/syslog:syslog
